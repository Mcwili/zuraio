name = "zuraio-contact-relay"
main = "src/index.ts"
compatibility_date = "2024-11-01"

[vars]
ENVIRONMENT = "production"

# Uncomment after zuraio.ch is on Cloudflare and api.zuraio.ch is proxied to Workers:
# routes = [{ pattern = "api.zuraio.ch/api/contact", zone_name = "zuraio.ch" }]
